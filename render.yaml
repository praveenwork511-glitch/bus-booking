services:
  - type: web
    name: bus-booking
    runtime: python
    pythonVersion: 3.9
    buildCommand: bash build.sh
    startCommand: gunicorn --bind 0.0.0.0:$PORT wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
      - key: FLASK_ENV
        value: production
